package main;

/**
 * Class which contains the main method for the core module. It does not do anything but a main method is required for
 * the module to be executable.
 */
public class CoreMain {
    /**
     * Main method for the core module. It does not do anything but a main method is required for the module to be
     * executable and packaged into a jar.
     *
     * @param args command line arguments
     */
    public static void main(String[] args) {
    }
}
